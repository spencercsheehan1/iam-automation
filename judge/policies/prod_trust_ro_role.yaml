# Jury: the eligibility policy for PROD_TRUST_RO_ROLE.
#
# A user is eligible for `role` only when every condition under
# `eligibility` matches their current employee attributes. Comparisons
# are case-insensitive.

version: "1.0"
role: PROD_TRUST_RO_ROLE

eligibility:
  employment_status: active
  department:
    - TRUST
    - GRC
